# universal-source-coding

A new modified procedure for LZW following the footsteps of tree structured lempel ziv welch algorithm but leveraging a better compression rate than conventional LZ78 algorithm.

## Usage

Just configure the main.py file and change the `file` fields and run:

```python
python3.xx main.py
```
